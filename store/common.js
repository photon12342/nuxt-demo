export const state = () => ({
  breads: [
    // {route: '/', name: '首页'},
    // {route: '/dashboard', name: 'dashboard'},
  ],
})

export const mutations = {
  setBreads(state, data) {
    state.breads = data;
  },
}

export const getters = {
  
}

export const actions = {
 
}